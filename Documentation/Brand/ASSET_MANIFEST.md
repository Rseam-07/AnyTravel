# AnyTravel visual asset manifest

| Asset | Source | Palette | License / provenance |
| --- | --- | --- | --- |
| `folded-horizon.svg` and exported app icon | Authored directly for AnyTravel as an original SVG mark | `#126E66`, `#FFF4E8`, `#F55C33` | Original project work; no model output, third-party artwork, font, or trademark asset used |
| `migratory-path.svg` | Original concept exploration; not shipped | Same project palette | Original project work |
| `river-compass.svg` | Original concept exploration; not shipped | Same project palette | Original project work |

The selected “折叠远方” mark combines an unfolded map, a warm horizon, and a dotted route moving across the folds. It was checked at 40, 60, 120, 256, and 1024 pixels. The source remains editable SVG; the Xcode asset catalog uses the 1024-pixel PNG export.
