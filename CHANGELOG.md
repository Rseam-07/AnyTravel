# Changelog

## 0.5.4 — 2026-08-31

旅程写完以后，也该有一页可以带在身边。

- 完整方案页新增原生导出入口，可以分享带地图的多页 PDF、可导入系统日历的 `.ics`，也可以把两种文件一起带走。
- PDF 会依次收拢旅行条件、地图、每日时刻与地址、住宿、往返大交通、门到门接驳和详细费用；跨页内容有连续标题，不会把一天的安排悄悄截断。
- 地图只绘制 Apple Maps 已返回的路线。快照暂时不可用时仍保留完整文字方案，并明确说明，不补画虚构道路。
- 日历为每个时间段建立独立事件，带上地点与行程说明；没有出发日期时仍可导出 PDF，但日历入口会提示先补日期。
- 文件生成期间显示轻量状态，完成后进入 iOS 系统分享页，并以触觉反馈回应；系统“减少动态效果”设置继续生效。

验证：29 个 iOS 单元测试、8 个 XCUITest 与 9 个 Node 测试全部通过；自动化样本生成 4 页 A4 PDF 和 15 个日历事件，PDF 每页完成渲染检查。模拟器真实生成带地图的方案文件并进入系统分享页，确认“存储到文件”可用。0.5.4（8）arm64 设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。日历使用导出设备的当前时区；精美长图、网页预览与可分享链接仍在后续清单中。

## 0.5.3 — 2026-08-31

火车停靠之后，旅程不该在站台上断成两截。

- 选定大交通与住宿后，iOS 会分别查询去程枢纽到住处、住处到返程枢纽的地铁公交、打车和步行方案。
- 接驳卡片显示耗时、距离、同行总费用与推荐状态；抵达较晚、同行人数较多或适合步行时，推荐会随真实条件变化。
- Apple Maps 返回的路线明确标成“地图路线”。公交路线暂缺时只显示“距离估算”并提示出发前复核，不绘制虚构路线，也不让估算结果压过已取得的打车路线。
- 选择接驳方式后，背景地图会切换到对应路线；去程与返程分别选择、分别保存，恢复旅册后仍保留。
- 费用表新增“抵达接驳”和“返程接驳”，并从原有市内交通额度中扣除，避免同一段路被计算两次。
- 交通面板重新整理成可滚动层级，让地图、往返车次和门到门接驳在小屏幕上仍然清楚可点。

验证：27 个 iOS 单元测试、7 个 XCUITest 与 9 个 Node 测试全部通过；模拟器真实点击完成“住宿 → 去程接驳选择 → 返程接驳选择 → 费用”流程。联网实查“苏州站→苏州吴宫泛太平洋酒店”取得 18 分钟、6.98 公里的驾车路线和 98 分钟、6.27 公里的步行路线；当前环境的公交查询返回 MapKit 无可用路线，App 按预期显示距离估算。0.5.3（7）arm64 设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。接驳价格均为本地估算，并非地图或网约车平台实时成交价；公交距离估算不会绘制成真实线路。

## 0.5.2 — 2026-08-31

去时有清晨的窗，归途也该有自己的时刻。

- 铁路 12306 现在会按抵达日和返程日分别查询，返程自动调转始发与到达城市；任一方向短暂失败时，另一方向仍可继续返回。
- 交通页加入去程/返程分段选择。两边拥有独立车次、选中状态、推荐理由、余票、票价和购买入口，切换时地图同步聚焦车站与住宿。
- 费用表会按各自所选车次分别计算去程和返程；只有返程没有实时结果时才沿用去程价格预留，并明确标注它不是返程实时报价。
- 保存行程会同时保存去程与返程选择；恢复旧版本旅册仍然兼容，复核暂时失败时保留上次结果并显示抓取时间与失败方向。
- 返程卡片重新整理了标题、耗时、接驳距离和空结果文案，并继续使用可中断弹簧动画与系统“减少动态效果”降级。

验证：23 个 iOS 单元测试与 7 个 XCUITest 全部通过；模拟器真实点击完成“住宿 → 去程 → 返程 → 费用”流程。9 个 Node 测试通过；联网实查“上海⇄苏州”分别返回 8 个去程和 8 个返程可购车次，两边均取得余票与展示价格。0.5.2（6）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。当前实时往返覆盖铁路；航班、长途客运和自驾仍以推荐模型与渠道入口为主，不会显示成已经取得的实时票价。

## 0.5.1 — 2026-08-31

一天装不下的念想，现在可以安静地挪到旅程的另一页。

- 每一站的菜单都能把地点移动或复制到另一天；复制会生成独立停靠，之后可以分别调整。
- 跨天操作会同时使两天的路线失效，并在编辑完成后重新核算住宿均距、枢纽接驳与报价。
- 不允许把某一天移成空白，也不会向目标日重复放入同一地点；失败原因会直接留在编辑器里。
- 新增最多 30 步的撤销与重做，覆盖搜索添加、删除、当天排序、跨天移动与复制；恢复后地图与报价状态一起回到对应版本。
- 编辑器顶部加入清晰的撤销/重做操作，天数胶囊实时显示停靠数量，并继续遵循系统“减少动态效果”。

验证：20 个 iOS 单元测试与 7 个 XCUITest 全部通过；跨天复制、删除、撤销、重做和条件重算均完成真实点击验证。0.5.1（5）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。编辑历史只保留在当前编辑会话中，退出编辑器后会清空，不会制造跨启动的虚假版本历史。

## 0.5.0 — 2026-08-31

这一版开始把 iOS 从“看见一份方案”推进到“能继续把方案改成自己的”。

- 方案生成后可以直接搜索 Apple Maps 真实地点，加入当天行程；已有停靠支持拖动排序、前后移动和删除，不必回到开头重新生成。
- 编辑过程会立即更新地图焦点并重算当前日路线；结束编辑后，住宿均距、枢纽接驳和报价会随新的景点分布重新核算。
- 新增生成后的旅行条件编辑，可单独修改日期、出发地、同行人数、预算、市内方式、抵达偏好，以及是否跳过住宿或大交通。
- 报价不再静默失败。界面明确区分缺少日期、未连接节点、刷新中、最新结果、缓存结果、部分成功、无匹配和服务失败，并显示可执行的下一步。
- 实时价显示抓取新鲜度，超过 30 分钟提示复核；恢复本地旅册时保留方案，同时自动复核地图与价格。
- 平台网页登录改为由有效 Cookie 证明会话已经留下；网页尚未建立会话时不会给出虚假的“已保存”。
- 费用表拆开去程实时报价与返程预算，返程未核价时明确说明；跳过大交通后金额归零。

验证：18 个 iOS 单元测试与 6 个 XCUITest 全部通过，覆盖首次进入、方案标签、保存恢复、生成后编辑和条件重算；0.5.0（4）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。铁路节点目前只返回去程实时班次，返程单列为预算预留；跨天移动、复制、撤销与重做留在后续版本。

## 0.4.0 — 2026-08-31

“折叠远方”第一次同时落在 iOS 与 Android 的地图上。

- 新增 Android 12 及以上原生客户端，使用 Kotlin、Jetpack Compose、MapLibre 与 OpenFreeMap。
- 四页欢迎流程采用自然语义断句，首屏标题为“下一次旅行，你想前往哪里？”，并保存常用出发地、预算与人数。
- 目的地、日期、天数、预算、兴趣、强度、住宿和交通可以按任意顺序修改；默认每天两处主要停留。
- 地图作为全屏背景，规划卡片、日程、住宿、交通和费用在同一底部面板中联动；用户拖动地图后自动镜头会让出控制。
- 苏州内置人工核对的地点与枢纽包；其他目的地通过 Android 系统地理编码服务查找景点、酒店、车站和机场。
- 接入既有开源报价节点。Android 实测取得 RollingGo 酒店每晚价、抓取时间与购买链接，以及铁路 12306 车次、余票、席别价格和官方入口。
- 新增行程保存/恢复/删除、节点健康检查、失败提示、深浅色语义、48dp 操作区与系统移除动画降级。
- APK 按 ABI 拆分，同时提供推荐的 arm64 包和兼容性更高的通用包。

验证：4 个 Android 规划逻辑单元测试、1 个 Android 12 端到端 Compose 流程和 Android lint 通过；欢迎页、苏州地图、完整方案与实时住宿/交通卡片均完成模拟器视觉检查。

发布边界：0.4.0 APK 使用 Android 调试签名，适合预览与侧载；还不是应用商店生产签名。Android 首版彩色路线表示游览顺序，尚未冒充逐路段道路导航。

## 0.3.0 — 2026-08-31

这一版让地图上的每一次选择都有更清晰、连贯的回应。

- 建立统一动效系统：欢迎页、进度胶囊、底部面板、方案标签、卡片选择和地图标记使用可中断的弹簧过渡。
- 路线继续逐段铺开，住宿、交通和费用切换时同步移动地图镜头；报价刷新与加载状态加入轻量循环反馈。
- 完整支持系统“减少动态效果”，会停用位移、缩放、脉冲和循环旋转，保留清晰的淡入淡出与状态变化。
- RollingGo 改为对 MapKit 酒店候选逐店实时查询，最多并行 8 家，并修正 MCP 结构化价格解析。
- RollingGo 返回多晚总价时会换算成每晚价；只有酒店名强匹配时才附加报价，避免错价贴卡。
- 欢迎页加入随页面流动的柔和色块；首屏和完整方案页重新调整文案断句与视觉节奏。
- 新增两段模拟器动效实录，覆盖首次启动和地图方案联动。

联网验收：3 个苏州酒店候选均取得 RollingGo 实时展示价与购买链接；私有密钥只保存在 Git 忽略的本机环境文件中。

发布验收：13 个 iOS 单元测试、4 个 XCUITest、8 个 Node 测试全部通过；0.3.0（3）Release 模拟器构建通过。

## 0.2.0 — 2026-08-31

“折叠远方”是 AnyTravel 第一个可以走完整段旅程的版本。

- 加入四页首次启动流程：应用介绍、顺序自由的规划逻辑、完整方案说明，以及默认出发地/人数/预算设置。
- 支持在应用内连接携程、去哪儿、Trip.com 与铁路 12306 网页会话，并复用到报价或预订页面。
- 住宿与交通条件可以任意顺序填写，也可以先留白；默认行程强度保持轻松。
- 地图新增酒店、民宿、机场、高铁站和地铁站标记，并随卡片选择自动调整视野。
- 完整方案加入逐日时间表、住宿候选、交通推荐、报价卡和明细费用表。
- 新增自建报价节点：RollingGo 酒店接口、需用户登录的携程网页采集，以及只读的铁路 12306 余票和价格查询。
- 所有报价展示渠道、价格口径与抓取时间；演示数据不会参与费用计算。
- 采用原创“折叠远方”App Icon 和更温暖的中文文案。

验证：13 项 iOS 单元测试、4 项 XCUITest、4 项 Node 测试；另完成铁路 12306 实时查询和 Release 模拟器构建。
