# Changelog

## Web 0.1 — 2026-09-02（开发中）

浏览器版 AnyTravel 的第一个可用闭环：React + TypeScript + Vite + MapLibre GL。桌面为 Apple Maps 式大屏横排（左侧面板 + 全屏地图 + 底部行程轨道），移动端为可拖拽三档底部面板。

- 目的地检索经伴随节点代理 OSM 地理编码；条件（出发地、日期、天数、人数、预算、节奏、兴趣、市内移动）即改即用。
- 方案生成：OpenStreetMap POI（Overpass 经节点，含真实营业时间）为景点源，空间聚类 + 近邻排序 + 时间表（停留/移动/午餐/夜游/机动），超载明确提示；OSRM 可用时以真实路网耗时替换直线估算。
- 一周天气预报（Open-Meteo，直连）；住宿目录与实时比价（RollingGo 实测真实价格，渠道健康逐项显示）；铁路 12306 去返程真实班次票价余票；去哪儿门票公开起价回填（实测拙政园 ¥80 等）。
- 内置 DeepSeek 对话：浏览器直连、白名单动作（目的地/日期/人数/预算/节奏/兴趣/交通），回复展示自然语言，失败给出可恢复提示；本机解析回退数字与节奏。
- 费用表（实时/估算分级）与预算对比；旅册、分享链接（URL 还原）、打印/PDF、地图样式切换、定位、北向、深浅色。
- 新增 Backend `/v1/places/poi`（Overpass，多镜像 + 缓存 + 负缓存）与 `/v1/places/geocode`（Nominatim 代理，带 UA 与限速）。

验证：桌面端完整走查（欢迎→目的地→生成 3 天方案→住宿 RollingGo 真价→交通→费用→DeepSeek 对话→设置渠道健康 9 项）与移动端三档面板均通过；生产构建通过。诚实边界见 [Web/README.md](Web/README.md)。

## 0.7.0 — 开发中（未发布）

先挑想去的地方，再让地图看见这一刻的价格。

- 生成方案前新增“先挑想去的地方”：同时检索热门景点、必去地标与偏好分类（MapKit 为主，高德 Web 服务补充），按在线名次、评分与空间分布去重排序；可以勾选主游览点，也可以交给 App 代选。勾选的主游览点在编排时获得更长的停留，选少了会顺路补齐，超过舒适容量时明确提示而不会静默删减。
- 住宿报价改为“一家酒店多渠道”结构：同一条目录合并 RollingGo 城市与景点锚点、携程登录会话、同程/艺龙、艺龙开放平台与万邦携程目录；卡片显示“N家比价”、来源标签、房型/床型/早餐/取消政策，并在数量口径一致时才把最低有效 offer 标成实时价。
- 住宿价格现可直连：App 内置 `ANYTRAVEL_ROLLINGGO_API_KEY` 时无需自建节点即可调用 RollingGo 酒店 MCP；伴随服务接通时仍优先走节点，两路共享同一套每晚价换算与去重。
- 铁路 12306 可以在 App 内直连：先按官方车站表归一化站名，再去程/返程分别读取余票车次与席别价；节点与应用内两路合并，单边失败保留另一边与明确诊断，购买入口始终回到官方购票页。
- 航班价格接入去哪儿公开移动航班页：在 WebKit 中与登录会话共用同一网站数据存储，读取列表卡片与接口 JSON，得到当前页面展示起价与“当日起价”；安全验证、无航班两种状态明确分开。节点侧新增携程航班通道（默认关闭，与酒店共用持久会话）。
- 节点新增艺龙开放平台酒店通道（官方签名、按日期 `LowRate`）与万邦（OneBound）携程目录通道（仅供目录与参考价）；健康检查逐项报告 `ctripSession`、`ctripFlights`、`tongchengSession`、`elongOpenAPI` 与 `oneBoundCtrip` 状态，未配置凭据如实显示 `disabled`。
- 主面板改为内容自适应：不同阶段、地图焦点、住宿/交通数量与加载状态会得到贴合内容的面板高度，拖动松手后按速度与位置落到最近档位；地图操作后自动重新适配。
- 智能向导的托管模式支持 App 内置 `ANYTRAVEL_ZAI_API_KEY`（无节点时直连智谱 OpenAI 兼容接口）；高德地点补充同样支持内置 Web 服务 Key 直连回退。

验证边界：65 个 iOS 单元测试（含去哪儿公开航班页联网探针）与 16 个 XCUITest 全部通过，完整 iOS 套件在 iPhone 17 Pro / iOS 26.5 模拟器为 81/81，0 失败、0 跳过；Backend 42/42 单元测试通过，其中含艺龙开放平台、万邦携程目录与携程航班解析测试。艺龙开放平台、万邦携程目录与携程航班通道尚未配置凭据，仍未联网验收。

## 0.6.3 — 2026-09-01

地图留在身后，旅程可以轻轻拉开，也可以只留一句尚未说完的话。

- 主面板改为 Apple 地图式三档交互：拖动过程中连续跟手，松手按速度与位置落到“仅输入框”“主要内容”或“几乎全屏”；轻点把手与辅助功能增减手势也可切换。
- 最小档只保留开放式自然语言输入；最大档展开完整日程，并自动收起会压住内容的路线状态、定位、地图样式和北向控件。重置、设置和旅册始终可见。
- 重新排好同行人数与日期：每一项的减号、当前值和加号都在同一行，抵达日与返程日均可前后调整，禁用边界有清晰的视觉状态。
- 自动规划在空间聚类前合并跨来源重复地点；同名同址与近距离名称变体保留信息更丰富的一条，相距较远的同名分店不会被误删。手动跨天复制仍按用户选择保留。
- 补齐全局地图动作：定位、地图样式、北向、设置、旅册与重新规划均有实际响应、触觉反馈和可访问名称；修复 iOS 26 Liquid Glass 下 SF Symbol 视觉按钮只有图标轮廓可点击的问题。
- 深浅色模式完成三档面板、输入框与顶栏的实机尺寸截图检查；最大档不再出现地图控件和状态胶囊覆盖行程卡片。

验证：57 个 iOS 单元测试、16 个 XCUITest 与 29 个 Node 测试全部通过；完整 iOS 套件在 iPhone 17 Pro / iOS 26.5 模拟器为 73/73，0 失败、0 跳过。设备 Release 构建产物为 0.6.3（12）。

发布边界：IPA 未签名，需要自签后侧载。定位按钮会触发系统定位授权；未授权或暂时取不到位置时会保留当前旅程视野并明确提示。跨来源去重仅处理高置信的名称与空间匹配，不会用模糊名称合并远距离分店。

## 0.6.2 — 2026-09-01

地图开始听懂一整句话，也终于在夜色里看清每一个按钮。

- 首页改为开放式自然语言输入。出发地、目的地、日期、天数、人数、预算、节奏、兴趣、交通方式、住宿价格和排序可以任意组合；托管模型、自定义 OpenAI 兼容模型与本机解析回退共用同一套受控动作。
- 修复覆盖整页的透明布局截获点击问题；日期、筛选、标签和编辑按钮统一提供至少 44pt 点击区域与按压反馈。抵达日和返程日都可前后调整，并同步校正天数与住宿晚数。
- 重建浅色/深色语义色。改用资源目录动态色，避免 Swift 6 在 SwiftUI 异步渲染期间解析 `UIColor` provider 造成崩溃；输入区提高不透明度，深色地图路线和卡片保持可读。
- 住宿目录同时查询城市酒店、精品酒店、连锁酒店、民宿、公寓酒店、青旅与每日景点簇；再合并 RollingGo 城市和最多三个景点锚点结果，去重后最多展示 40 家。
- 酒店卡片新增品牌、星级、图片、设施、标签、官网、实时价与抓取时间，并支持综合、低价、近景点、近交通、每晚上限、只看实时价和只看官网筛选。
- 修正 RollingGo 公开工具的 `size` 上限和价格语义：`lowestPrice` 不再被误当多晚总价，只有明确总价字段才按晚数换算。本轮苏州联网样本由 4 个查询合并得到 18 家带价格酒店。
- 完成圆周旅迹 3.5.0 静态模块审计，新增 AI 对话、编辑、复制、协作、链接导入、分享和足迹功能差距矩阵；同时记录 TripStar、RollingGo、携程查价、酒店对比与亚朵筛选项目的实际采用点。

验证：53 个 iOS 单元测试、12 个 XCUITest 与 29 个 Node 测试全部通过；iPhone 17 Pro 模拟器完成深色首页/完整方案视觉检查、自由输入、酒店筛选、日期前后调整、跨天复制和撤销/重做。设备 Release 构建产物为 0.6.2（11）。

发布边界：IPA 未签名，需要自签后侧载。RollingGo 搜索价仍需在选择房型或锁价时复核；携程和同程可能要求用户在持久浏览器会话中登录或完成平台验证。去哪儿酒店、Trip.com 和大多数酒店官网当前提供核价入口，尚未宣称已取得同房型实时报价。

## 0.6.0 — 2026-08-31

远方不必一次塞满；一句话，也可以让地图重新舒展。

- 完整方案会判断每天停留数、路途时间与失败路段。当行程显得太赶时，一键“替我铺成松弛旅程”会保留全部地点和先后顺序，将主要停留尽量控制在每天两处，并同步调整结束日期。
- 节奏变化后不沿用旧结论：逐日路线、住宿晚数、返程班次、门到门接驳和费用都会重新查询；地图以弹簧动效展开新的日程，系统减少动态效果时改用温和过渡。
- 新增地图上的智能向导。默认由自建伴随服务托管 GLM-5.3-Flash；设置中也可切换到用户自己的 OpenAI 兼容 Base URL、模型与 API Key，自定义 Key 只保存到本机钥匙串。
- 模型只能调整节奏、市内交通、预算，以及聚焦或移除行程里已经存在的地点。服务端与 iOS 各自复核一次白名单，无法安全执行时只返回文字，不凭空向地图添加地点。
- 新增高德 Web 服务地点适配器，保留来源、原始 GCJ-02 坐标及转换后的 WGS84 语义；高德不可用时 Apple Maps 搜索仍独立完成。当前收到的 Key 返回平台类型不匹配，界面不会把它写成已接通。
- 同程加入 iOS 平台网页登录、会话识别和住宿、航班、客运购买入口。对用户提供的同程、去哪儿、携程 Android 包只做静态互操作审计，记录公开包名、签名摘要、网页域名和深链，不把第三方私有身份材料打进 AnyTravel。
- 补充三端演进方案：共享伴随服务、数据契约、测试夹具与设计令牌，iOS/Android/Web 保留各自原生地图与交互层，减少重复业务开发，同时维持平台体验。

验证：38 个 iOS 单元测试、10 个 XCUITest 与 17 个 Node 测试全部通过；iPhone 17 Pro 模拟器真实完成行程松弛重排与模型设置流程。伴随服务联网调用 GLM-5.3-Flash 成功，返回的节奏、公交和现有地点聚焦动作均通过双重白名单。0.6.0（9）arm64 设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。高德地点补充需要控制台中平台类型为“Web 服务”的 Key；当前提供的 Key 返回 `USERKEY_PLAT_NOMATCH (10009)`，本版会自动回退 Apple Maps。默认 GLM 与高德 Key 均不进入客户端或 Git 历史。

## 0.5.4 — 2026-08-31

旅程写完以后，也该有一页可以带在身边。

- 完整方案页新增原生导出入口，可以分享带地图的多页 PDF、可导入系统日历的 `.ics`，也可以把两种文件一起带走。
- PDF 会依次收拢旅行条件、地图、每日时刻与地址、住宿、往返大交通、门到门接驳和详细费用；跨页内容有连续标题，不会把一天的安排悄悄截断。
- 地图只绘制 Apple Maps 已返回的路线。快照暂时不可用时仍保留完整文字方案，并明确说明，不补画虚构道路。
- 日历为每个时间段建立独立事件，带上地点与行程说明；没有出发日期时仍可导出 PDF，但日历入口会提示先补日期。
- 文件生成期间显示轻量状态，完成后进入 iOS 系统分享页，并以触觉反馈回应；系统“减少动态效果”设置继续生效。

验证：29 个 iOS 单元测试、8 个 XCUITest 与 9 个 Node 测试全部通过；自动化样本生成 4 页 A4 PDF 和 15 个日历事件，PDF 每页完成渲染检查。模拟器真实生成带地图的方案文件并进入系统分享页，确认“存储到文件”可用。0.5.4（8）arm64 设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。日历使用导出设备的当前时区；精美长图、网页预览与可分享链接仍在后续清单中。

## 0.5.3 — 2026-08-31

火车停靠之后，旅程不该在站台上断成两截。

- 选定大交通与住宿后，iOS 会分别查询去程枢纽到住处、住处到返程枢纽的地铁公交、打车和步行方案。
- 接驳卡片显示耗时、距离、同行总费用与推荐状态；抵达较晚、同行人数较多或适合步行时，推荐会随真实条件变化。
- Apple Maps 返回的路线明确标成“地图路线”。公交路线暂缺时只显示“距离估算”并提示出发前复核，不绘制虚构路线，也不让估算结果压过已取得的打车路线。
- 选择接驳方式后，背景地图会切换到对应路线；去程与返程分别选择、分别保存，恢复旅册后仍保留。
- 费用表新增“抵达接驳”和“返程接驳”，并从原有市内交通额度中扣除，避免同一段路被计算两次。
- 交通面板重新整理成可滚动层级，让地图、往返车次和门到门接驳在小屏幕上仍然清楚可点。

验证：27 个 iOS 单元测试、7 个 XCUITest 与 9 个 Node 测试全部通过；模拟器真实点击完成“住宿 → 去程接驳选择 → 返程接驳选择 → 费用”流程。联网实查“苏州站→苏州吴宫泛太平洋酒店”取得 18 分钟、6.98 公里的驾车路线和 98 分钟、6.27 公里的步行路线；当前环境的公交查询返回 MapKit 无可用路线，App 按预期显示距离估算。0.5.3（7）arm64 设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。接驳价格均为本地估算，并非地图或网约车平台实时成交价；公交距离估算不会绘制成真实线路。

## 0.5.2 — 2026-08-31

去时有清晨的窗，归途也该有自己的时刻。

- 铁路 12306 现在会按抵达日和返程日分别查询，返程自动调转始发与到达城市；任一方向短暂失败时，另一方向仍可继续返回。
- 交通页加入去程/返程分段选择。两边拥有独立车次、选中状态、推荐理由、余票、票价和购买入口，切换时地图同步聚焦车站与住宿。
- 费用表会按各自所选车次分别计算去程和返程；只有返程没有实时结果时才沿用去程价格预留，并明确标注它不是返程实时报价。
- 保存行程会同时保存去程与返程选择；恢复旧版本旅册仍然兼容，复核暂时失败时保留上次结果并显示抓取时间与失败方向。
- 返程卡片重新整理了标题、耗时、接驳距离和空结果文案，并继续使用可中断弹簧动画与系统“减少动态效果”降级。

验证：23 个 iOS 单元测试与 7 个 XCUITest 全部通过；模拟器真实点击完成“住宿 → 去程 → 返程 → 费用”流程。9 个 Node 测试通过；联网实查“上海⇄苏州”分别返回 8 个去程和 8 个返程可购车次，两边均取得余票与展示价格。0.5.2（6）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。当前实时往返覆盖铁路；航班、长途客运和自驾仍以推荐模型与渠道入口为主，不会显示成已经取得的实时票价。

## 0.5.1 — 2026-08-31

一天装不下的念想，现在可以安静地挪到旅程的另一页。

- 每一站的菜单都能把地点移动或复制到另一天；复制会生成独立停靠，之后可以分别调整。
- 跨天操作会同时使两天的路线失效，并在编辑完成后重新核算住宿均距、枢纽接驳与报价。
- 不允许把某一天移成空白，也不会向目标日重复放入同一地点；失败原因会直接留在编辑器里。
- 新增最多 30 步的撤销与重做，覆盖搜索添加、删除、当天排序、跨天移动与复制；恢复后地图与报价状态一起回到对应版本。
- 编辑器顶部加入清晰的撤销/重做操作，天数胶囊实时显示停靠数量，并继续遵循系统“减少动态效果”。

验证：20 个 iOS 单元测试与 7 个 XCUITest 全部通过；跨天复制、删除、撤销、重做和条件重算均完成真实点击验证。0.5.1（5）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。编辑历史只保留在当前编辑会话中，退出编辑器后会清空，不会制造跨启动的虚假版本历史。

## 0.5.0 — 2026-08-31

这一版开始把 iOS 从“看见一份方案”推进到“能继续把方案改成自己的”。

- 方案生成后可以直接搜索 Apple Maps 真实地点，加入当天行程；已有停靠支持拖动排序、前后移动和删除，不必回到开头重新生成。
- 编辑过程会立即更新地图焦点并重算当前日路线；结束编辑后，住宿均距、枢纽接驳和报价会随新的景点分布重新核算。
- 新增生成后的旅行条件编辑，可单独修改日期、出发地、同行人数、预算、市内方式、抵达偏好，以及是否跳过住宿或大交通。
- 报价不再静默失败。界面明确区分缺少日期、未连接节点、刷新中、最新结果、缓存结果、部分成功、无匹配和服务失败，并显示可执行的下一步。
- 实时价显示抓取新鲜度，超过 30 分钟提示复核；恢复本地旅册时保留方案，同时自动复核地图与价格。
- 平台网页登录改为由有效 Cookie 证明会话已经留下；网页尚未建立会话时不会给出虚假的“已保存”。
- 费用表拆开去程实时报价与返程预算，返程未核价时明确说明；跳过大交通后金额归零。

验证：18 个 iOS 单元测试与 6 个 XCUITest 全部通过，覆盖首次进入、方案标签、保存恢复、生成后编辑和条件重算；0.5.0（4）设备 Release 构建通过。

发布边界：IPA 未签名，需要自签后侧载。铁路节点目前只返回去程实时班次，返程单列为预算预留；跨天移动、复制、撤销与重做留在后续版本。

## 0.4.0 — 2026-08-31

“折叠远方”第一次同时落在 iOS 与 Android 的地图上。

- 新增 Android 12 及以上原生客户端，使用 Kotlin、Jetpack Compose、MapLibre 与 OpenFreeMap。
- 四页欢迎流程采用自然语义断句，首屏标题为“下一次旅行，你想前往哪里？”，并保存常用出发地、预算与人数。
- 目的地、日期、天数、预算、兴趣、强度、住宿和交通可以按任意顺序修改；默认每天两处主要停留。
- 地图作为全屏背景，规划卡片、日程、住宿、交通和费用在同一底部面板中联动；用户拖动地图后自动镜头会让出控制。
- 苏州内置人工核对的地点与枢纽包；其他目的地通过 Android 系统地理编码服务查找景点、酒店、车站和机场。
- 接入既有开源报价节点。Android 实测取得 RollingGo 酒店每晚价、抓取时间与购买链接，以及铁路 12306 车次、余票、席别价格和官方入口。
- 新增行程保存/恢复/删除、节点健康检查、失败提示、深浅色语义、48dp 操作区与系统移除动画降级。
- APK 按 ABI 拆分，同时提供推荐的 arm64 包和兼容性更高的通用包。

验证：4 个 Android 规划逻辑单元测试、1 个 Android 12 端到端 Compose 流程和 Android lint 通过；欢迎页、苏州地图、完整方案与实时住宿/交通卡片均完成模拟器视觉检查。

发布边界：0.4.0 APK 使用 Android 调试签名，适合预览与侧载；还不是应用商店生产签名。Android 首版彩色路线表示游览顺序，尚未冒充逐路段道路导航。

## 0.3.0 — 2026-08-31

这一版让地图上的每一次选择都有更清晰、连贯的回应。

- 建立统一动效系统：欢迎页、进度胶囊、底部面板、方案标签、卡片选择和地图标记使用可中断的弹簧过渡。
- 路线继续逐段铺开，住宿、交通和费用切换时同步移动地图镜头；报价刷新与加载状态加入轻量循环反馈。
- 完整支持系统“减少动态效果”，会停用位移、缩放、脉冲和循环旋转，保留清晰的淡入淡出与状态变化。
- RollingGo 改为对 MapKit 酒店候选逐店实时查询，最多并行 8 家，并修正 MCP 结构化价格解析。
- RollingGo 返回多晚总价时会换算成每晚价；只有酒店名强匹配时才附加报价，避免错价贴卡。
- 欢迎页加入随页面流动的柔和色块；首屏和完整方案页重新调整文案断句与视觉节奏。
- 新增两段模拟器动效实录，覆盖首次启动和地图方案联动。

联网验收：3 个苏州酒店候选均取得 RollingGo 实时展示价与购买链接；私有密钥只保存在 Git 忽略的本机环境文件中。

发布验收：13 个 iOS 单元测试、4 个 XCUITest、8 个 Node 测试全部通过；0.3.0（3）Release 模拟器构建通过。

## 0.2.0 — 2026-08-31

“折叠远方”是 AnyTravel 第一个可以走完整段旅程的版本。

- 加入四页首次启动流程：应用介绍、顺序自由的规划逻辑、完整方案说明，以及默认出发地/人数/预算设置。
- 支持在应用内连接携程、去哪儿、Trip.com 与铁路 12306 网页会话，并复用到报价或预订页面。
- 住宿与交通条件可以任意顺序填写，也可以先留白；默认行程强度保持轻松。
- 地图新增酒店、民宿、机场、高铁站和地铁站标记，并随卡片选择自动调整视野。
- 完整方案加入逐日时间表、住宿候选、交通推荐、报价卡和明细费用表。
- 新增自建报价节点：RollingGo 酒店接口、需用户登录的携程网页采集，以及只读的铁路 12306 余票和价格查询。
- 所有报价展示渠道、价格口径与抓取时间；演示数据不会参与费用计算。
- 采用原创“折叠远方”App Icon 和更温暖的中文文案。

验证：13 项 iOS 单元测试、4 项 XCUITest、4 项 Node 测试；另完成铁路 12306 实时查询和 Release 模拟器构建。
