# AnyTravel Web — local settings. Never commit real values.
# Optional: compile-time DeepSeek key for the in-app chat assistant.
# Users can also paste their own key in the in-app settings (stored locally).
VITE_DEEPSEEK_API_KEY=
